# 포트폴리오 내용 수정 방법

이 웹사이트의 텍스트, 프로젝트, 연락처, 프로필 이미지는 `content.js`에서 수정합니다.
디자인과 레이아웃은 `styles.css`, 동작은 `script.js`, HTML 기본 구조는 `index.html`에 있습니다.

## 웹페이지에서 바로 수정하기

기본 공유 페이지에서는 편집 버튼이 보이지 않습니다.
수정할 때는 상단 왼쪽의 로고/타이틀 영역을 첫 클릭부터 5초 이내에 10번 누르면 편집 모드가 열립니다.
5초가 지나면 카운트가 초기화되고, 다음 클릭부터 새로 계산됩니다.

편집 모드가 열리면 페이지 왼쪽 하단에 편집 도구가 나타나고, 주요 텍스트를 화면에서 바로 수정할 수 있습니다.

- `닫기`: 현재 수정 내용을 브라우저에 저장하고 편집 도구를 숨깁니다.
- `저장`: 현재 브라우저에 수정 내용을 저장하고 편집 모드는 유지합니다.
- `파일 저장`: 수정 내용을 `content.js` 파일로 저장합니다. 파일 선택 창이 뜨면 이 폴더의 `content.js`를 선택해 덮어쓰면 됩니다.
- 프로필 이미지 영역 클릭: 이미지 파일을 선택해 프로필 이미지를 바꿉니다.
- 프로젝트의 `Demo`, `GitHub` 아래 URL 입력칸: 각 버튼에 연결될 주소를 수정합니다.
- 하단 연락처: 표시 텍스트는 직접 수정하고, URL 입력칸에서 실제 연결 주소를 수정합니다.
- `초기화`: 브라우저에 저장된 수정 내용을 지우고 기본 내용으로 되돌립니다.

브라우저 보안상 웹페이지가 프로젝트 폴더를 자동으로 열 수는 없습니다.
대신 `파일 저장`은 브라우저가 마지막 저장 위치를 기억할 수 있도록 설정되어 있습니다.
처음 한 번 이 폴더의 `content.js`를 선택하면, 다음 저장부터 같은 위치가 열릴 가능성이 높습니다.
브라우저가 직접 파일 저장 기능을 지원하지 않는 경우에는 `content.js` 파일이 다운로드됩니다.

## 자주 수정하는 위치

- 이름: `profile.name`
- 직무 소개: `profile.role`
- 프로필 이미지: `profile.image`
- 첫 화면 제목: `hero.title`
- 자기소개: `about.paragraphs`
- 기술 스택: `skills.items`
- 프로젝트: `projects.items`
- 성장과정: `growth.timeline`
- 연락처: `contact.links`

## 이미지 넣는 방법

1. 이 폴더 안에 `images` 폴더를 만듭니다.
2. 예: `images/profile.jpg` 파일을 넣습니다.
3. `content.js`에서 아래처럼 수정합니다.

```js
profile: {
  name: "홍길동",
  role: "Frontend Developer",
  image: "images/profile.jpg",
  avatarText: "DEV",
}
```

`image` 값을 빈 문자열 `""`로 두면 이미지 대신 `avatarText`가 표시됩니다.

## 프로젝트 링크 넣는 방법

프로젝트의 `links`에서 `url` 값을 채우면 실제 링크 버튼으로 바뀝니다.
비워두면 `준비중` 버튼으로 표시됩니다.

```js
links: [
  { label: "Demo", url: "https://example.com" },
  { label: "GitHub", url: "https://github.com/your-id/project" },
]
```

## 프로젝트 추가 방법

`projects.items` 배열 안에 기존 프로젝트 블록 하나를 복사해서 붙여 넣고 내용을 바꾸면 됩니다.
쉼표가 빠지면 페이지가 표시되지 않을 수 있으니, 각 프로젝트 블록 사이의 쉼표를 확인하세요.
